# Local Docker overrides
# IMPORTANT: keep the async driver (`+asyncpg`).
DATABASE_URL=postgresql+asyncpg://ctf_user:ctf_pass@db:5432/ctf_db

# JWT config
JWT_SECRET=supersecretfortheCTF
JWT_ALGORITHM=HS256
JWT_EXPIRY_MINUTES=60
